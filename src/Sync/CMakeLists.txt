ADD_SUBDIRECTORY(Spinlock)


ADD_LIBRARY(Sync INTERFACE)

TARGET_INCLUDE_DIRECTORIES(Sync INTERFACE
  $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
  $<INSTALL_INTERFACE:include/Sync>
)

TARGET_LINK_LIBRARIES(Sync INTERFACE
  Spinlock
  third_party_futex_like
)
